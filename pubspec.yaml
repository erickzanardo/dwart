name: dwart
description: A package to generate Dwarfs names
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.7.0 <4.0.0"

dev_dependencies:
  coverage: ^1.15.0
  mocktail: ^1.0.4
  test: ^1.31.0
  very_good_analysis: ^8.0.0
